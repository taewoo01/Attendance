# PLAY GROUND — Agent Instructions

> 이 문서는 PLAY GROUND 프로젝트의 AI 코딩 에이전트 작업 기준이다.
> 프로젝트의 기존 아키텍처와 기술 스택을 우선하며, 모든 작업은 최소 변경과 검증을 원칙으로 한다.

---

## 0. Agent 작업 원칙

### 0.1 기본 원칙

* 요청된 작업 범위만 수정한다.
* 관련 없는 리팩터링을 하지 않는다.
* 기존 아키텍처와 폴더 구조를 우선 재사용한다.
* 기존 코드가 정상적으로 동작한다면 구조를 임의로 변경하지 않는다.
* 새로운 dependency는 반드시 필요한 경우에만 추가한다.
* 기존 API, DB schema, 타입을 임의로 변경하지 않는다.
* mock 데이터로 실제 구현 문제를 우회하지 않는다.
* 작업과 관련 없는 파일은 수정하지 않는다.
* 파일 삭제 및 이름 변경은 작업에 반드시 필요한 경우에만 수행한다.
* 보안 요구사항은 기능 구현보다 우선한다.

### 0.2 최소 변경 원칙

작업은 가능한 한 작은 diff로 해결한다.

다음 작업은 사용자의 명시적인 요청이 없다면 수행하지 않는다.

* 전체 코드 리팩터링
* 폴더 구조 변경
* dependency 교체
* 대규모 네이밍 변경
* DB schema 재설계
* API 구조 재설계
* 디자인 시스템 교체
* 관련 없는 코드 정리
* 사용하지 않는 코드의 임의 삭제

더 나은 구조가 보이더라도 현재 작업에 필요하지 않다면 그대로 유지한다.

---

## 1. 코드 탐색 원칙

Claude는 작업 시작 시 프로젝트 전체를 무작정 탐색하지 않는다.

### 탐색 순서

1. 현재 작업 요구사항을 확인한다.
2. `docs/TASKS.md`가 존재하면 현재 작업을 먼저 확인한다.
3. 작업과 직접적으로 관련된 파일을 찾는다.
4. 해당 파일의 import/export 및 dependency를 확인한다.
5. 필요한 경우에만 연결된 모듈을 추가로 탐색한다.
6. 작업과 무관한 디렉터리와 파일은 읽지 않는다.

### 탐색 범위

가능하면 다음 순서로 범위를 확장한다.

```text
현재 작업 파일
    ↓
직접 import/export 관계
    ↓
관련 service / repository / component
    ↓
관련 schema / API
    ↓
필요한 경우에만 프로젝트 전체 구조
```

프로젝트 전체를 이해해야 한다는 이유만으로 전체 파일을 읽지 않는다.

---

## 2. 구현 원칙

### 2.1 기존 코드 우선

기능 구현 전에 기존에 동일하거나 유사한 기능이 있는지 확인한다.

* 기존 utility가 있으면 재사용한다.
* 기존 component가 있으면 재사용한다.
* 기존 service/repository 패턴을 따른다.
* 동일한 로직을 새로 만들지 않는다.

### 2.2 타입 안정성

* TypeScript strict 환경을 유지한다.
* `any` 사용은 필요한 경우를 제외하고 피한다.
* 기존 타입을 최대한 재사용한다.
* 타입 오류를 `as any` 등의 방식으로 숨기지 않는다.

### 2.3 에러 처리

* 예상 가능한 오류는 명시적으로 처리한다.
* 서버 오류와 사용자 입력 오류를 구분한다.
* 에러를 무시하거나 빈 `catch`로 숨기지 않는다.
* 디버깅을 위한 불필요한 `console.log`를 production 코드에 남기지 않는다.

---

## 3. 작업 절차

모든 개발 작업은 다음 순서를 따른다.

```text
요구사항 확인
    ↓
관련 파일 탐색
    ↓
기존 구현 확인
    ↓
최소 범위 수정
    ↓
관련 테스트
    ↓
typecheck
    ↓
lint
    ↓
build
    ↓
결과 확인
```

작업 중 예상하지 못한 구조적 문제가 발견되더라도 먼저 현재 작업을 안전하게 완료할 수 있는지 판단한다.

대규모 변경이 필요하다면 임의로 진행하지 않고 사용자에게 보고한다.

---

## 4. 검증 원칙

코드를 작성했다고 작업이 완료된 것으로 판단하지 않는다.

가능한 경우 다음 검증을 수행한다.

* 관련 테스트
* typecheck
* lint
* build

### 검증 실패

검증이 실패하면:

1. 에러 메시지를 확인한다.
2. 실패 원인과 관련된 코드만 수정한다.
3. 동일한 검증을 다시 실행한다.
4. 다른 기능에 영향을 주지 않았는지 확인한다.

검증을 통과시키기 위해 다음 행동을 하지 않는다.

* 테스트 삭제
* 테스트 조건 완화
* 타입 오류 숨기기
* lint 규칙 우회
* 에러 무시
* 기능을 임시 mock으로 대체

---

## 5. 보안 원칙

다음 사항은 항상 준수한다.

* `service_role` key는 서버에서만 사용한다.
* `service_role` key를 클라이언트 코드에 포함하지 않는다.
* 환경변수의 secret 값을 소스 코드에 하드코딩하지 않는다.
* RLS를 우회하기 위한 임시 구현을 만들지 않는다.
* private Storage 파일을 public URL로 노출하지 않는다.
* 사용자 입력을 HTML로 직접 렌더링하지 않는다.
* `dangerouslySetInnerHTML`은 사용하지 않는다.
* SQL 문자열을 직접 조합하지 않는다.
* 인증 및 권한 검증을 클라이언트에만 의존하지 않는다.
* 파일 업로드 시 서버 측 검증을 수행한다.

보안과 편의성이 충돌하는 경우 보안을 우선한다.

---

## 6. 변경 범위 관리

작업을 시작하기 전에 변경 대상 범위를 판단한다.

### 허용

```text
요구사항
  ↓
관련 파일 수정
  ↓
관련 테스트 수정/추가
```

### 지양

```text
요구사항
  ↓
관련 파일 수정
  ↓
기존 구조 리팩터링
  ↓
공통 컴포넌트 변경
  ↓
다른 기능 수정
  ↓
여러 파일 정리
```

후자의 변경이 반드시 필요하지 않다면 수행하지 않는다.

---

## 7. Dependency 관리

새로운 npm package를 추가하기 전에 기존 dependency로 해결할 수 있는지 먼저 확인한다.

새 dependency가 필요한 경우:

1. 기존 package로 해결할 수 없는지 확인한다.
2. 프로젝트 기술 스택과 호환되는지 확인한다.
3. 보안 및 유지보수 상태를 확인한다.
4. 필요한 경우 사용자에게 dependency 추가를 보고한다.

불필요한 package 추가를 금지한다.

---

## 8. 데이터 및 DB 원칙

* 기존 schema와 관계를 우선 확인한다.
* 동일한 데이터를 중복 저장하지 않는다.
* DB 변경이 필요한 경우 관련 schema와 migration을 함께 확인한다.
* RLS가 적용된 테이블은 RLS 정책을 고려하여 구현한다.
* 클라이언트에서 보안상 중요한 DB 검증을 수행하지 않는다.
* DB 오류를 무시하고 성공 응답을 반환하지 않는다.

---

## 9. 응답 원칙

작업 완료 후 응답은 간결하게 작성한다.

포함할 내용:

```text
1. 변경 파일
2. 핵심 변경 내용
3. 검증 결과
```

불필요한 설명이나 장황한 코드 해설은 작성하지 않는다.

---

# PLAY GROUND — 기술 스택 & 보안 요구사항

내부 팀 협업 플랫폼(출석/일정/실적/아이디어/자료실/팀 소개) 구현을 위한 기술 스택과 보안 요구사항 정리.

이 문서를 기준으로 프로젝트를 세팅하고 개발을 진행한다.

---

## 10. 기술 스택

| 영역      | 선택                                    | 비고                                                               |
| ------- | ------------------------------------- | ---------------------------------------------------------------- |
| 프레임워크   | **Next.js (App Router) + TypeScript** | 서버 컴포넌트로 CRUD 페이지 구성. SSR/SEO는 불필요하지만 API route/서버 액션 분리 목적으로 사용 |
| DB      | **Supabase (PostgreSQL)**             | 무료 티어로 소규모 팀 운영 충분                                               |
| ORM     | **Drizzle**                           | SQL에 가까운 스키마 정의로 AI 코딩 툴의 diff 리뷰가 쉬움                            |
| 인증      | **Supabase Auth**                     | 이메일 매직링크 권장, 비밀번호 방식도 가능                                         |
| 파일 저장소  | **Supabase Storage**                  | private 버킷 + presigned URL                                       |
| 스타일링    | **Tailwind CSS**                      | 기존 목업의 CSS 변수를 Tailwind theme으로 이전                               |
| 데이터 동기화 | **react-query + 짧은 staleTime 폴링**     | 인원 8명 규모라 websocket/Realtime 불필요                                 |
| 배포      | **Vercel + Supabase**                 | 무료 티어로 시작                                                        |

### 페이지 ↔ 데이터 매핑

| 페이지       | 주요 테이블                                     |
| --------- | ------------------------------------------ |
| 출석 인증     | `attendance`                               |
| 상주 시간 등록  | `presence`                                 |
| 개인 일정     | `personal_events`                          |
| 고정 시간표    | `fixed_schedules`                          |
| 실적 관리     | `achievements`                             |
| 아이디어 모음집  | `ideas`, `idea_comments`, `idea_reactions` |
| 회사 소개     | `company_info`                             |
| 팀원 소개     | `profiles`                                 |
| 자료실       | `files`                                    |
| 회의록       | `meeting_notes`                            |
| 데일리 업무 정리 | `daily_logs`                               |

---

## 11. 보안 요구사항

### 11.1 Supabase 키 관리

* `service_role` 키는 서버 사이드에서만 사용한다.
* 클라이언트 번들에 절대 포함하지 않는다.
* 클라이언트에는 필요한 경우 `anon` 키만 노출한다.
* `.env.local`은 `.gitignore`에 포함한다.
* secret 값을 코드에 하드코딩하지 않는다.

### 11.2 RLS

* 모든 테이블은 기본적으로 RLS를 활성화한다.
* 기본 정책은 차단을 원칙으로 한다.
* 필요한 정책만 명시적으로 허용한다.
* 테이블 생성 시 RLS 정책을 함께 설계한다.

#### 기본 정책

* `attendance`, `presence`, `personal_events`, `daily_logs`

  * INSERT/UPDATE/DELETE: 본인만 허용
  * SELECT: 팀 전체 허용

* `ideas`, `idea_comments`

  * SELECT/INSERT: 팀 전체 허용
  * UPDATE/DELETE: 본인 작성 데이터만 허용

* `company_info`

  * 공개 데이터와 내부 데이터를 분리한다.
  * 공개 데이터에만 `anon` SELECT를 허용한다.

* `profiles`

  * 내부 연락처 등 민감한 정보는 `anon`에게 노출하지 않는다.

### 11.3 QR 출석 체크

정적 QR 사용을 금지한다.

시간 기반 토큰 방식을 사용한다.

* QR 토큰은 짧은 주기로 재발급한다.
* 체크인 요청 시 서버에서 토큰 만료 여부를 검증한다.
* 토큰 중복 사용 여부를 검증한다.
* 필요한 경우 연구실 내부 IP/Wi-Fi 확인을 보조 조건으로 검토한다.

### 11.4 파일 업로드

* Storage bucket은 private으로 설정한다.
* 다운로드는 만료 시간이 있는 presigned URL을 사용한다.
* 서버에서 확장자 allowlist를 검증한다.
* 파일 용량 제한을 적용한다.
* 실행 파일 업로드를 차단한다.

### 11.5 인증

팀 규모를 고려하여 이메일 매직링크 방식을 우선 검토한다.

비밀번호 방식을 사용할 경우 Supabase Auth의 brute-force 방지 설정을 확인한다.

### 11.6 입력값 처리

* React 기본 escaping을 사용한다.
* `dangerouslySetInnerHTML`을 사용하지 않는다.
* SQL 문자열을 직접 조합하지 않는다.
* Drizzle의 parameterized query를 사용한다.

### 11.7 Dependency

* 불필요한 dependency를 추가하지 않는다.
* 신규 package는 유지보수 상태와 보안을 확인한다.
* `npm audit`을 주기적으로 실행한다.

### 11.8 백업

Supabase 무료 티어의 백업 및 PITR 제한을 고려한다.

주요 데이터는 주기적으로 export한다.

대상:

* 출석
* 실적
* 일정
* 아이디어
* 기타 복구가 필요한 핵심 데이터

---

## 12. 초기 보안 체크리스트

* [ ] 전 테이블 RLS 정책 설계 및 적용
* [ ] `service_role` key 서버 전용 격리 확인
* [ ] QR 출석 토큰 만료/검증 로직 설계
* [ ] 공개 페이지 데이터 분리
* [ ] Storage private 설정
* [ ] presigned URL 발급 로직
* [ ] 인증 방식 최종 결정
* [ ] `.gitignore` / 환경변수 커밋 여부 확인

---

## 13. 작업 완료 기준

기능 구현 시 다음 조건을 만족해야 완료로 판단한다.

* [ ] 요구사항 구현
* [ ] 기존 기능 정상 동작
* [ ] 관련 테스트 통과
* [ ] TypeScript typecheck 통과
* [ ] lint 통과
* [ ] production build 통과
* [ ] 보안 요구사항 위반 없음
* [ ] 불필요한 파일 변경 없음
* [ ] 불필요한 dependency 추가 없음
